# Container Sandbox (auto-generated)

This is an **ephemeral Linux arm64 sandbox** (Debian bookworm) for Python development.
The container is destroyed on exit — all local changes are lost.

**The only way to persist your work is to commit and push to the remote repository
(or create a pull request via `gh pr create`).**

You have full freedom to experiment: install packages, modify files, run tests,
break things. Nothing here affects the host or any shared environment.

## Environment

- User: `sandbox` (home: `/home/sandbox`)
- Project files: `/workspace` (isolated copy of the host project)
- OS: Debian bookworm, Linux arm64
- Python: `{{PYTHON_VERSION}}`
- Claude Code: `{{CLAUDE_VERSION}}`

## Permissions

This sandbox may run with permission prompts disabled or with plan-mode
escalation enabled. Either way, you can work freely — the environment is
isolated and ephemeral. If you need to escalate permissions during a plan-mode
session, inform the user and they can approve.

## Available Tools

- git, gh (GitHub CLI), jq, ripgrep, fd, fzf, uv
- openssh-client (SSH keys pre-configured for github.com)
- You can install additional packages with `sudo apt-get install -y <package>`
  or Python packages with `uv pip install <package>`

## Python Development

### Virtual environment setup

The host `.venv/` contains macOS binaries — do not use it. Create a Linux virtualenv:

```bash
uv venv .venv && source .venv/bin/activate
uv pip install -r requirements.txt   # or: uv pip install -e .
```

### ANTHROPIC_API_KEY conflict

`ANTHROPIC_API_KEY` is set to an empty string in this container (for Claude Code
OAuth). If your project uses `load_dotenv()`, it will NOT override the empty value.
Change every `load_dotenv()` call to:

```python
load_dotenv(override=True)
```

If `ANTHROPIC_BASE_URL` is set to a proxy in the container environment, add this
to `.env`:

```
ANTHROPIC_BASE_URL=https://api.anthropic.com
```

### Build artifacts

All compiled extensions and native binaries are Linux arm64 — they will not run
on macOS.
