# Container build configuration
# Used by launch.sh when --rebuild is enabled.
# Override path with: --config PATH or CONTAINER_BUILD_CONFIG env var.
# Version changes here affect newly built images only.

[versions]
# Use "latest" to resolve dynamically at build time.
# Pin versions for reproducible images.
claude_code = "latest"
codex = "latest"
gh = "2.87.3"
fd = "10.4.0"
python = "3.14"
go = "1.26.1"
golangci_lint = "v2.11.1"

[builder]
# Resources allocated to the container builder process.
# Env vars BUILD_CPUS / BUILD_MEMORY override these.
# Useful when rebuilds are slow or fail with OOM.
cpus = 2
memory = "4g"

[features]
# Install godoc-mcp for Go documentation via MCP (Go containers only).
# Default is OFF. Rebuild required when toggled.
install_godoc_mcp = false
# Install OpenAI Codex CLI for code analysis via MCP.
# Default is OFF. Rebuild required when toggled.
install_codex = false
